import Link from"next/link";export default async function AlertStatus({params}:{params:Promise<{status:string}>}){const{status}=await params;const content:Record<string,[string,string]>={confirmed:["Alerts confirmed","Your double opt-in is complete. Reset Oracle will only send the alerts you selected."],unsubscribed:["Unsubscribed","You will not receive further Reset Oracle alerts."],invalid:["Link unavailable","This token is invalid or expired. Request a new confirmation from the forecast page."]};const[x,y]=content[status]??content.invalid;return<main className="status-page"><p className="eyebrow">RESET ORACLE / ALERTS</p><h1>{x}</h1><p>{y}</p><Link href="/">Return to forecast →</Link></main>}
